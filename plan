
job spec link
    - scrap the site for the job spec
    - ask llm
        - most importants things in job spec
        - now modify job titles and resume bullets to more closely correlate with it - without changing any information
        - pick two specific projects to add to the project section
        - add additional keywords to the end

resume
    - provide latex template
    - provide static info (name, address, jobs, projects) in json? ig
    - make sure to use STAR
    - if more than one page -> repeat until 1 page

cover letter
    - provide latex template
    - use generated resume + static info to make cover letter
    - provide amazing cover letter examples as model
    - make sure human

answer additional questions:
    - provide question get answer based on rest 


flow: 
    provide job spec => gives resume => gives cover letter
        provide add question => gives back in either pdf or plain text
