[project]
name = "resume-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bs4>=0.0.2",
    "logger>=1.4",
    "pdflatex>=0.1.3",
    "requests>=2.33.1",
]
